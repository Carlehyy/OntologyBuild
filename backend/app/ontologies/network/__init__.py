"""本体网络 — 跨本体全局图视图（只读）。

数据源是 PostgreSQL ``fo_*`` 投影 / 发布快照，不依赖 Neo4j 投影就绪；
每个本体优先取 ``current_release_id`` 的已发布口径，未发布本体回退读
工作区实时数据并在响应中显式标注。跨本体之间目前不存在合法的关系边，
全局网络是"各本体子图同画布叠加 + 可选的同名类型虚线桥接（纯展示层
启发式，不落库）"，语义级跨本体映射属于独立的数据建模需求。

路由模块由组合根（app/main.py）按架构守卫规则显式导入，
本包 ``__init__`` 刻意保持为空，避免非组合根依赖 router 模块。
"""
