"""Independent Super Assistant runtime.

This package deliberately depends only on platform primitives (auth, database,
model configuration and encryption). It must not import ontology or exploration
business modules.
"""
